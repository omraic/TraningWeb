/*int 型の変数 x に任意の数値を代入し、
インクリメント演算子、デクリメント演算子を適用して結果を表示することにより
演算子の効果を確認するプログラムを作成しなさい。*/

#include <stdio.h>

int main() {
	int x;
	printf("xの値を入力してください。\n");
	scanf_s("%d",&x);


	printf("x=%d\n",x);
	printf("x+1は%d\n", ++x );
	x--;
	printf("x-1は%d\n",--x);

	return 0;
}
