/*個数を示す数値を入力し、その個数分だけ‘*’を表示するプログラムを作成しなさい。*/

#include <stdio.h>

int main(void) {
	int num;
	printf("個数を入力してください。\n");
	scanf_s("%d",&num);
	for (int i = 0; i < num;i++) {
		printf("*");

	}
	printf("\n");

	return 0;
}
