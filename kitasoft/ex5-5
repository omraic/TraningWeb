/*整数を 10 回入力し、平均値を求めるプログラムを作成しなさい。*/

#include <stdio.h>

int main(void) {
	int ave = 0;
	int num;
	printf("10回整数を入力してください。\n");
	for (int i = 1; i < 11; i++) {
		printf("%d回目の入力です。\n",i);
		scanf_s("%d",&num);
		ave += num;
	}
	ave /= 10;

	printf("平均は%d\n",ave);
	return 0;
}
