/*
九九表（一の段～九の段）を表示するプログラムを作成しなさい。
※ 	printf(" %2d", x ); のように、%2d と記述すると表示が 2 桁に揃う。
*/

#include <stdio.h>

int main(void){
	int num = 1;

	for (int i = 1; i < 10;i++) {
		for (int k = 1; k < 10;k++) {
			printf("%d \t",num*k);
		}
		num++;
		printf("\n");
	}
	return 0;
}
