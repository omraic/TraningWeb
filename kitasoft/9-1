/*
整数の 2 乗を計算する関数を作成しなさい。１つの int 型引数を取り、結果を戻り値として返すこと。
また、関数の動作を検証できるようにプログラムを作成しなさい。
*/

#include <stdio.h>
int Sq(int x);

int main(void) {
	int num;

	scanf_s("%d",&num);

	int ans = Sq(num);


	printf("%d",ans);
	return 0;
}

int Sq(int x){
	int i = x * x;

	return i;
}
