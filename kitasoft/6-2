/*
次のプログラムを作成しなさい。

10 個の数値を入力する。
入力された順番と逆の順番で 10 個の数値を表示する。
*/

#include <stdio.h>

int main(void) {
	int num[10];
	printf("数値を10回入力してください。\n");
	for (int i = 9; i >= 0;i--) {
		scanf_s("%d",&num[i]);
	}


	for (int i = 0; i < 10;i++) {
		printf("%d ",num[i]);
	}
	printf("\n");

	return 0;
}
