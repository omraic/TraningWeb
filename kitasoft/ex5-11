/*個数を示す数値を入力し、その個数分だけ 0 ～ 9 の数字を表示するプログラムを作成しなさい。
数字は 0 , 1 , 2 , 3 , , の順に表示し、9 の次は 0 に戻るものとします。*/

#include <stdio.h>

int main(void) {
	int num;
	printf("数値を入力してください。\n");
	scanf_s("%d",&num);

	for (int i = 0; i < num;i++) {
		printf("%d",i%10);
	}
	printf("\n");

	return 0;
}
