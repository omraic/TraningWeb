/*
九九のひとつの段を表示する関数を作成しなさい。何の段（ 1 ～ 9 ）であるかを引数とします。

その関数を使用して、九九表を作成しなさい。
*/

#include <stdio.h>
int kuku(int x);

int main(void) {
	for (int i = 1; i < 10;i++) {
		kuku(i);
		printf("\n");
	}

	return 0;
}

int kuku(int x) {
	for (int i = 1; i < 10;i++) {
		printf("%d ",x*i);
	}
}
